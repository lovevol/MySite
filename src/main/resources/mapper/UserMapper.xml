<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.UserMapper">
    <select id="selectUserByLoginName" parameterType="string" resultType="model.User">
        SELECT * FROM user WHERE loginname = #{loginName}
    </select>
    <insert id="save" parameterType="model.User" useGeneratedKeys="true">
        INSERT INTO user(username, loginname,password, gender,roletype,email) VALUES(#{userName},#{loginName},#{password},#{gender},#{roleType},#{email})
    </insert>
    <select id="selectUserById" parameterType="int" resultType="model.User">
        SELECT * FROM user WHERE iduser = #{id}
    </select>
    <update id="updateUserById" parameterType="model.User">
        UPDATE user
        SET username = #{userName},password = #{password},age = #{age}
        WHERE id = #{id}
    </update>
    <delete id="deleteUserById" parameterType="int">
        DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>